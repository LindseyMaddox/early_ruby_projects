<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="row">
          <div class="col-sm-3">
            <div class="navbar-header">Workouts</div>
          </div>
          <div class="col-sm-8"></div>
          <div class="col-sm-1">
            <button type="button" class="btn btn-info navbar-btn btn-xs"><%= link_to "Log Out", log_out_path %></button>
          </div>
        </div>
    </div>
</nav>
<div class="container">
  <% if logged_in? %>
    <div class="row header">
      <div class="col-sm-1"></div>
      <div class="col-sm-5"><%= @workout.date_completed %> Workout </div>
    </div>
	</br>
    <div class="row">
      <div class="col-sm-1"></div>
      <div class="col-sm-6">Did <em><%= @workout.video.name %></em> for <%= @workout.video.length %> minutes.</div>
    </div>
</div>
</br>
  <div class="row">
  	<div class="col-sm-1"></div>
    <div class ="col-sm-5">
      <button type="button" class="btn btn-primary"><%= link_to 'Edit Workout', edit_workout_path(@workout) %></button>
      <button type="button" class="btn btn-primary"><%= link_to 'Workout Record', workouts_path %></button>
    </div>
  </div>
  <% else %>
    <div class="row">
        <div class="col-sm-1"></div>
        <div class="col-sm-8"><em>You must be <strong><%= link_to "logged in", log_in_path %></strong> to see this page.</em></div>
  <% end %>
</div>